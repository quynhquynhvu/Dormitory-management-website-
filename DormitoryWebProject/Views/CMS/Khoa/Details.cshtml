@model DormitoryWebProject.Models.Khoa

@{
    ViewBag.Title = "Details";
    ViewBag.formheader = "";
    Layout = "~/Views/Shared/Main/_LayoutFormCMS.cshtml";
}
<div class="card card-dark">
    <div class="card-header">
        <h3 class="card-title">Chi tiết @Model.TenKhoa</h3>
    </div>
    <div class="card-body">
        <div class="row justify-content-end">
            <a href="/Khoa/Edit/@Model.MaKhoa" class="btn btn-default btn-flat bg-indigo"><i class="ionicons ion-edit"></i> Chỉnh sửa</a>
            <a href="#" class="btn btn-default btn-flat bg-danger ml-1" data-toggle="modal" data-target="#deleteconfirmation"><i class="far fa-trash-alt"></i> Xóa</a>
            <div class="form-horizontal container">
                <div class="form-group row">
                    <label class="control-label col-md-2">@Html.DisplayNameFor(model => model.TenKhoa)</label>
                    <div class="col-md-4">
                        @Model.TenKhoa
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="card-footer">
        <a href="/Khoa" class="btn btn-default bg-navy"><i class="ionicons ion-ios-undo-outline"></i> Trở về</a>
    </div>
</div>

<div class="modal fade" id="deleteconfirmation">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Xác nhận xóa?</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form action="/Khoa/Delete/@Model.MaKhoa" method="post" style="margin:0;">
                    @Html.AntiForgeryToken()
                    <div>
                        Are you sure you want to delete this item?
                    </div>
                    <div class="mt-3 text-right">
                        <button type="button" class="btn btn-secondary btn-flat" data-dismiss="modal" onclick="closeModalWindow(); return false;">
                            Không, hủy bỏ
                        </button>
                        <button type="submit" class="btn btn-danger" value="Delete" data-commit-action="delete" data-commit-id="@Model.MaKhoa">
                            Xóa
                        </button>
                    </div>
                </form>
            </div>
        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div>

